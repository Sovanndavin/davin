# Usage

This is a small demo project. Files:

- `src/` - sample source files
- `docs/` - documentation

Steps used to create the repo are recorded in the project history (commits and tags `v1`, `v2`).
